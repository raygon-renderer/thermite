[package]
name = "thermite-ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "staticlib"] # Both dynamic and static

[dependencies]
paste = "1"
thermite = { path = "../thermite" }
libc = "0.2"

[build-dependencies]
cbindgen = "0.29"
