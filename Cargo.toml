[package]
name = "thermite"
version = "0.1.0"
authors = ["novacrazy <novacrazy@gmail.com>"]
edition = "2018"

[features]
default = ["alloc"]
alloc = []

[dependencies]
paste = "1.0.2"

[profile.release]
opt-level = 3
lto = 'fat'
codegen-units = 1

[features]
default = ["alloc"]
alloc = []